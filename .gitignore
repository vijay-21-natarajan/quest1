# Working directories. Recreated at runtime by config.ensure_dirs(), which is
# why they can be ignored wholesale -- nothing downstream handles a missing dir.
data/
tests/fixtures/

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Model weights download here on first run. Hundreds of MB; never commit.
.paddleocr/
.cache/

.DS_Store
.idea/
.vscode/